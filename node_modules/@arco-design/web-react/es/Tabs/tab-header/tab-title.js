import React from 'react';
import IconClose from '../../../icon/react-icon/IconClose';
import cs from '../../_util/classNames';
import IconHover from '../../_class/icon-hover';
import { isFunction } from '../../_util/is';
var TabHeaderTitle = function (_a, ref) {
    var _b;
    var prefixCls = _a.prefixCls, onDeleteTab = _a.onDeleteTab, tabKey = _a.tabKey, isActive = _a.isActive, onClickTab = _a.onClickTab, _c = _a.disabled, disabled = _c === void 0 ? false : _c, title = _a.title, editable = _a.editable, renderTitle = _a.renderTitle, deleteIcon = _a.deleteIcon, deleteButton = _a.deleteButton, getIdPrefix = _a.getIdPrefix, index = _a.index;
    var render = isFunction(renderTitle)
        ? renderTitle
        : function (node) {
            return node;
        };
    return render(React.createElement("div", { ref: ref, key: tabKey, className: cs(prefixCls + "-header-title", (_b = {},
            _b[prefixCls + "-header-title-active"] = isActive,
            _b[prefixCls + "-header-title-editable"] = editable,
            _b[prefixCls + "-header-title-disabled"] = disabled,
            _b)), onClick: function (e) {
            if (disabled)
                return;
            onClickTab(e);
        } },
        React.createElement("span", { className: prefixCls + "-header-title-text", role: "tab", "aria-selected": isActive, "aria-disabled": disabled || undefined, id: getIdPrefix(index).tab, "aria-controls": getIdPrefix(index).tabpane }, title),
        editable && (React.createElement("span", { role: "button", "aria-label": "remove tab", "aria-disabled": disabled || undefined, tabIndex: disabled ? -1 : 0, className: prefixCls + "-close-icon", onClick: function (e) {
                e.stopPropagation();
                if (disabled)
                    return;
                onDeleteTab(e);
            } }, deleteButton || React.createElement(IconHover, { prefix: prefixCls }, deleteIcon || React.createElement(IconClose, null))))), {
        key: tabKey,
        isActive: isActive,
        disabled: disabled,
        editable: editable,
    });
};
export default React.forwardRef(TabHeaderTitle);
